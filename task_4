users = ['user1', 'user2', 'user3', 'user1', 'user4', 'user2']

# TODO Добавьте словарь и замените в нем нулевые значения статисчикой посещений

user_amount = 0
unique_in = 0

user_amount = f"Общее количество: {len(users)}"
unique_in = f"Уникальные посещения: {len(set(users))}"

print({f'Общее количество': len(users), ""'Уникальные посещения': len(set(users))})
